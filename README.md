# Raspberry Pi OS

## Cập Nhật Hệ Thống
```bash
sudo apt update
sudo apt full-upgrade
sudo apt autoremove
sudo reboot
```

## Cài Đặt Docker
```bash
curl -sSL https://get.docker.com | sh
sudo usermod -aG docker $USER
logout
```
